## 2025-05-14 - Explicit Truncation Feedback
**Learning:** In CLI tools that truncate large file outputs, failing to notify the user can lead to confusion or incorrect assumptions about the file's content. Providing a clear, bracketed warning ensures the user is aware they are seeing a partial view.
**Action:** Always include a clear indicator or warning message when data is truncated in the UI or CLI output.
